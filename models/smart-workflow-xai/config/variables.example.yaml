# yaml-language-server: $schema=https://json-schema.axonivy.com/14.0-dev/config/variables.example.json
Variables:
  AI:
    Providers:
      xAI:
        #[password]
        APIKey: ${decrypt:your-encrypted-api-key}
        BaseUrl: https://my-xai-resource.com/
        # The default model, if not explicitly specified on the calling Agent.
        # [enum: grok-4-1-fast, grok-4-1-mini, grok-4-1-large, grok-4-1-max, grok-4-1-mini-code, grok-4-1-large-code, grok-4-1-max-code]
        DefaultModel: grok-4-1-fast
